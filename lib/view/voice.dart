import 'package:flutter/material.dart';

// Voice Screen
class VoiceScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(

      body: Center(child: Text('This is the Voice Screen')),
    );
  }
}